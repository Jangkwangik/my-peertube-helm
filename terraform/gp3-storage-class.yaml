apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: gp3
  annotations:
    storageclass.kubernetes.io/is-default-class: "true" # 기본값으로 설정
provisioner: ebs.csi.aws.com # ⭐ 우리가 힘들게 고친 그 드라이버!
volumeBindingMode: WaitForFirstConsumer
parameters:
  type: gp3
  fsType: ext4