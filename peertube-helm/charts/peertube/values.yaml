nameOverride: ""
fullnameOverride: ""
replicaCount: 1

image:
  registry: "docker.io"
  repository: chocobozzz/peertube
  pullPolicy: IfNotPresent
  tag: "v6.3.3" # PeerTube 안정 버전 명시

imagePullSecrets: []

containerCommand:
  - gosu
  - peertube
  - npm
  - start

extraEnv: []
envFrom: []
initcontainers: {}
podAnnotations: {}
podLabels: {}
podSecurityContext: {}
securityContext: {}
extraVolumes: []
extraVolumeMounts: []
tolerations: []
affinity: {}
nodeSelector: {}

# [1] 저장소 설정
persistence:
  enabled: true
  accessModes:
    - ReadWriteOnce
  storageClassName: "gp2"
  storage: "10Gi"
  existingClaim: ""

resources:
  limits:
    cpu: 2000m
    memory: 2Gi
  requests:
    cpu: 500m
    memory: 1Gi

livenessProbe:
  httpGet:
    path: /
    port: peertube

readinessProbe:
  httpGet:
    path: /
    port: peertube

autoscaling:
  enabled: false

serviceAccount:
  create: true
  automount: true
  annotations: {}
  name: "peertube-sa"

# [2] 서비스 로드밸런서 설정
service:
  enabled: true
  type: LoadBalancer
  port: 80

# [3] 인그레스 끄기
ingress:
  enabled: false
  className: "nginx"
  annotations: {}
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

# [4] PeerTube 앱 설정 (비밀번호 Base64 & 단순화)
peertube:
  log_level: info
  trust_proxy: '10.0.0.0/8'
  webserver_hostname: chart-example.local
  webserver_port: 80
  webserver_https: false
  secret: "Zml4ZWRzZWNyZXQ=" # fixedsecret (Base64)
  existingSecret: ""
  existingSecretKey: "secret"

  admin:
    email: "admin@example.com"
    password: "cGFzc3dvcmQ=" # password (Base64)
    existingSecret: ""
    # 👇 [수정됨] 에러 원인이던 Keys 설정 제거 (차트 기본값 사용)

  s3:
    enabled: false

  smtp:
    enabled: false

# [5] 외부 DB 사용 안 함
externalDatabase:
  enabled: false

externalValkey:
  enabled: false

# [6] 내부 DB 설정 (이미지 버전 강제 고정 & 비밀번호)
postgresql:
  enabled: true
  # 👇 [중요] 존재하지 않는 17.x 버전 대신 안정적인 16.x 버전 사용
  image:
    tag: "16.1.0-debian-11-r8"
  auth:
    database: peertube_production
    username: peertube
    password: "cGFzc3dvcmQ="
    postgresPassword: "cGFzc3dvcmQ="
    existingSecret: ""

# [7] 내부 캐시 설정 (이미지 버전 강제 고정 & 비밀번호)
valkey:
  enabled: true
  # 👇 [중요] 존재하지 않는 8.x 대신 호환되는 Redis 이미지 사용 고려했으나
  # Valkey 안정 버전 태그 지정
  image:
    tag: "7.2.4-debian-11-r2"
  auth:
    password: "cGFzc3dvcmQ="
    existingSecret: ""