nameOverride: ""
fullnameOverride: ""
replicaCount: 1

image:
  registry: "docker.io"
  repository: chocobozzz/peertube
  pullPolicy: IfNotPresent
  tag: ""

imagePullSecrets: []

containerCommand:
  - gosu
  - peertube
  - npm
  - start

extraEnv: []
envFrom: []
initcontainers: {}
podAnnotations: {}
podLabels: {}
podSecurityContext: {}
securityContext: {}
extraVolumes: []
extraVolumeMounts: []
tolerations: []
affinity: {}
nodeSelector: {}

# [1] 저장소 설정
persistence:
  enabled: true
  accessModes:
    - ReadWriteOnce
  storageClassName: "gp2"
  storage: "10Gi"
  existingClaim: ""

resources:
  limits:
    cpu: 2000m
    memory: 2Gi
  requests:
    cpu: 500m
    memory: 1Gi

livenessProbe:
  httpGet:
    path: /
    port: peertube

readinessProbe:
  httpGet:
    path: /
    port: peertube

autoscaling:
  enabled: false

serviceAccount:
  create: true
  automount: true
  annotations: {}
  name: "peertube-sa"

# [2] 서비스 로드밸런서 설정
service:
  enabled: true
  type: LoadBalancer
  port: 80

# [3] 인그레스 끄기
ingress:
  enabled: false
  className: "nginx"
  annotations: {}
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

# [4] PeerTube 앱 설정 (중요: 빈칸 없이 가짜 값이라도 채움)
peertube:
  log_level: info
  trust_proxy: '10.0.0.0/8'
  webserver_hostname: chart-example.local
  webserver_port: 80
  webserver_https: false
  secret: "fixedsecretkey12345" # 특수문자 제거한 단순 비밀번호
  existingSecret: ""
  existingSecretKey: "secret"

  admin:
    email: "admin@example.com"
    password: "fixedpassword123" # 특수문자 제거한 단순 비밀번호
    existingSecret: ""
    # 👇 [수정됨] 빈칸("") 대신 가짜 키 이름(dummy)을 넣음
    existingSecretKeys:
      email: "email"
      password: "password"

  s3:
    enabled: false
    endpoint: ""
    streaming_playlists_bucket: ""
    web_videos_bucket: ""
    user_exports_bucket: ""
    original_video_files_bucket: ""
    access_key_id: ""
    secret_access_key: ""
    existingSecret: ""
    # 👇 [수정됨] 안 써도 에러 방지용으로 채움
    existingSecretKeys:
      endpoint: "endpoint"
      streaming_playlists_bucket: "bucket1"
      web_videos_bucket: "bucket2"
      user_exports_bucket: "bucket3"
      original_video_files_bucket: "bucket4"
      access_key_id: "accesskey"
      secret_access_key: "secretkey"

  smtp:
    enabled: false
    host: ""
    port: ""
    user: ""
    password: ""
    from_address: ""
    existingSecret: ""
    # 👇 [수정됨] 안 써도 에러 방지용으로 채움
    existingSecretKeys:
      host: "host"
      port: "port"
      username: "username"
      password: "password"

# [5] 외부 DB 설정
externalDatabase:
  enabled: false
  hostname: "peertube-postgres-rw"
  database: "peertube"
  username: "peertube"
  password: "dummypassword" # 빈칸 금지
  existingSecret: ""
  existingSecretKeys:
    hostname: "hostname"
    database: "database"
    username: "username"
    password: "password"

externalValkey:
  enabled: false
  hostname: "valkey-primary"
  password: "dummypassword" # 빈칸 금지
  existingSecret: ""
  existingSecretKey: "valkey-password"

# [6] 내부 DB 설정
postgresql:
  enabled: true
  auth:
    database: peertube_production
    username: peertube
    password: "fixeddbpassword123" # 특수문자 제거
    postgresPassword: "fixeddbpassword123" # 특수문자 제거
    existingSecret: ""

# [7] 내부 캐시 설정
valkey:
  enabled: true
  auth:
    password: "fixedredispassword123" # 특수문자 제거
    existingSecret: ""
    existingSecretPasswordKey: ""